[phases.setup]
nixPkgs = ["nodejs", "ffmpeg", "python3"]

[phases.build]
cmds = ["npm install", "python3 -m venv /opt/venv && /opt/venv/bin/pip install yt-dlp"]

[phases.start]
cmds = ["PATH=\"/opt/venv/bin:$PATH\" node server.js"]
